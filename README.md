simpleserve
===========

One command to turn any directory into a static webserver


# install

` gem install simpleserve`

# usage

`simpleserve [port]`



========================

its that simple (tm)
